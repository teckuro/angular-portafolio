FROM node:18-alpine

# Instalar dependencias necesarias
RUN apk add --no-cache curl

# Crear directorio de trabajo
WORKDIR /app

# Instalar Railway CLI
RUN npm install -g @railway/cli@3.23.0

# Configurar el comando por defecto
CMD ["railway", "--help"]
